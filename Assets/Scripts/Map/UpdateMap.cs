using UnityEngine;
using System.Collections;

public class UpdateMap : MonoBehaviour {

    // Use this for initialization
    void Start () {
        Map.Instance.SetVisibility(true);
    }
    
    // Update is called once per frame
    void Update () {
        
    }
}
